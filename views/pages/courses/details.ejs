<!DOCTYPE html>

<html lang="en-us">
    <head>
        <% include ../../partials/head %>
    </head>

    <body>
        <header>
            <% include ../../partials/header %>
        </header>

        <main>
            <h2>Course Details</h2>
            <table>
                <tr>
                    <th>Course Details</th>
                    <th></th>
                </tr>
                <tr>
                    <td>Course Title</td>
                    <td><%= course.courseTitle %></td>
                </tr>
                <tr>
                    <td>Course Name</td>
                    <td><%= course.courseName %></td>
                </tr>
                <tr>
                    <td>Teacher</td>
                    <td><%= course.teacher %></td>
                </tr>
                <tr>
                    <td>Credits</td>
                    <td><%= course.credits %></td>
                </tr>
                <tr>
                    <td>Description</td>
                    <td><%= course.description %></td>
                </tr>
                <tr>
                    <td>Number of Students</td>
                    <td><%= course.studentCount %></td>
                </tr>
            </table><br>

            <a href="/courses/update/<%= ssnSemId %>/<%= course._id %>">Edit Course</a><br>
            <a href="/courses/delete/<%= ssnSemId %>/<%= course._id %>">Delete Course</a>

            <h2>Course Schedule</h2>
            <table>
                <tr>
                    <th>Day</th>
                    <th>Classroom</th>
                    <th>Start Time</th>
                    <th>End Time  </th>
                </tr>

                <% if(course.schedule.monday.isScheduled) { %>
                <tr>
                    <td>Monday</td>
                    <td><%= course.schedule.monday.classroom %></td>
                    <td><%= dateFormat(course.schedule.monday.startTime, "UTC:h:MM TT") %></td>
                    <td><%= dateFormat(course.schedule.monday.endTime, "UTC:h:MM TT") %></td>
                </tr>
                <% } %>
                
                <% if(course.schedule.tuesday.isScheduled) { %>
                <tr>
                    <td>Tuesday</td>
                    <td><%= course.schedule.tuesday.classroom %></td>
                    <td><%= dateFormat(course.schedule.tuesday.startTime, "UTC:h:MM TT") %></td>
                    <td><%= dateFormat(course.schedule.tuesday.endTime, "UTC:h:MM TT") %></td>
                </tr>
                <% } %>

                <% if(course.schedule.wednesday.isScheduled) { %>
                <tr>
                    <td>Wednesday</td>
                    <td><%= course.schedule.wednesday.classroom %></td>
                    <td><%= dateFormat(course.schedule.wednesday.startTime, "UTC:h:MM TT") %></td>
                    <td><%= dateFormat(course.schedule.wednesday.endTime, "UTC:h:MM TT") %></td>
                </tr>
                <% } %>
                
                <% if(course.schedule.thursday.isScheduled) { %>
                <tr>
                    <td>Thursday</td>
                    <td><%= course.schedule.thursday.classroom %></td>
                    <td><%= dateFormat(course.schedule.thursday.startTime, "UTC:h:MM TT") %></td>
                    <td><%= dateFormat(course.schedule.thursday.endTime, "UTC:h:MM TT") %></td>
                </tr>
                <% } %>
                
                <% if(course.schedule.friday.isScheduled) { %>
                <tr>
                    <td>Friday</td>
                    <td><%= course.schedule.friday.classroom %></td>
                    <td><%= dateFormat(course.schedule.friday.startTime, "UTC:h:MM TT") %></td>
                    <td><%= dateFormat(course.schedule.friday.endTime, "UTC:h:MM TT") %></td>
                </tr>
                <% } %>
            </table><br>

            <a href="/schedule/update/<%= ssnSemId %>/<%= course._id %>">Edit Schedule</a><br>
        </main>

        <footer>
            <% include ../../partials/footer %>
        </footer>
    </body>
</html>